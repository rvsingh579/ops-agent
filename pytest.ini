[pytest]
markers =
    slow: real end-to-end tests that hit the actual local LLM (seconds-to-
        minutes each, per the latency numbers we measured). Skipped by
        default. Run them explicitly with: pytest -m slow
        Run the full suite including these with: pytest -m "slow or not slow"
addopts = -m "not slow"
